# Reflact Common

Shared library containing data structures, networking packet definitions, and utilities used by both the server-side engine and the client-side mod to ensure consistency.

## Usage

This module is a dependency for both `client` and `engine`.

## Build

```bash
./gradlew publishToMavenLocal
```
